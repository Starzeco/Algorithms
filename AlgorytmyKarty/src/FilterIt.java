/*import java.util.ArrayList;

public class FilterIt implements IteratorArray {
	
	private IteratorArray it;
	private Predicate pred;
	
	private int pos=0;
	private ArrayList<Karta> array;
	
	 private int wartosc=0;  
	 private boolean bHasNext=true; 
	
	public FilterIt(IteratorArray it,Predicate pred) {
		this.it=it;
		this.pred=pred;
		array=new ArrayList<Karta>();
	}
	
/*	public void FindWiekszy() {
		while(it.hasNext()) {
			wartosc=it.next().getWartosc();
			int wartosc2=it.next().getWartosc();
			if(pred.Wiekszy(wartosc))
		}
	}
	
	public Karta next() {
		if(hasNext()) {
			return array.get(pos++);
		}
		else { 
			return null;
		}
	}
	public boolean hasNext() {
		return pos<array.size();
	}
	
}  */
